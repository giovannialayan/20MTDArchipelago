using UnityEngine;

public class CheckPopup : MonoBehaviour
{
    public void DisplayPopup(string message)
    {
        //instanitate prefab with worldtextpopup component
        // GameObject popup = Instantiate(Resources.Load<GameObject>("Prefabs/WorldTextPopup")); //check with unity explorer
        // popup.GetComponent<TextMeshPro>().text = message;
    }
}